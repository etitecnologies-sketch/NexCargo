# Normaliza line endings para LF em todos os arquivos de texto
# Evita conflitos entre Windows (CRLF) e Linux/Mac (LF)
* text=auto eol=lf

# Scripts shell sempre LF (para rodar no Linux)
*.sh text eol=lf

# Binários — não alterar
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
